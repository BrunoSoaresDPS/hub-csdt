# PostgreSQL connection string
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# JWT secret for authentication
JWT_SECRET=change_this_secret

# Application settings
NEXT_PUBLIC_APP_NAME=Hub CSDT
